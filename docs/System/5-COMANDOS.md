# Comandos de utilidad

**Para actualizar los themes desde DaisyUI:**

```bash
node resources/js/presets/fetch_all_themes.js
```

**Para compilar/construir los assets visuales (CSS/JS) locales a la carpeta dist:**

```bash
php artisan w4:build-native-assets
```
